import React, { useState } from "react";
import { withStyles } from "@material-ui/styles";
import Styles from "../jss/previewStyles";

function Preview() {
  const images = useState(() => {});

  return (
    <div>
      <p>jbdfsonh</p>
    </div>
  );
}

export default withStyles(Styles)(Preview);
